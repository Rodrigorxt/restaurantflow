terraform {
  backend "s3" {
    bucket       = "replace-with-terraform-state-bucket"
    key          = "restaurantflow/production/terraform.tfstate"
    region       = "us-east-1"
    encrypt      = true
    use_lockfile = true
  }
}

